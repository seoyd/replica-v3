[package]
name = "replica-commit-kernel"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
hex = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"

[dev-dependencies]
replica-v3 = { path = "../..", features = ["accelerate"] }

[patch.crates-io]
candle-core = { path = "../../vendor/candle-core-0.11.0" }
