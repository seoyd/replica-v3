# Selected-event protocol 1.1 — independent A/B evidence recheck

**A PASS; B execution integrity PASS; model quality FAIL;
NEXT_SIGNAL=NO_CLEAR_SIGNAL; GOAL1_READY=false.** No new confirmed code defect
was found in the reviewed protocol, inherited-state, scoring and closure paths.
No product repair or additional learning is requested. This closes the current
review; it does not reopen the closed F/I study.

The complete common contract and reviewer prompt for
`R3-SELECTED-EVENT-ID-PROTOCOL-1.1` were read. The repository's
replica-lean-development workflow was applied. The previous1.0
BLOCKED_INPUT_LENGTH decision and original evidence remain unchanged.

## Identity and evidence reuse

Reviewed and previously executed source is
`2e98f6a834ffe440ce2f20dec4361dee8818ef29`. Starting report HEAD and verified
remote were `6663981189a36de52430d2c436413b4568166744`. Source/tests/Cargo/vendor
have no difference from the candidate; the only pre-existing untracked item
was `.DS_Store`. The earlier three-file uncommitted draft and report HEAD44c9016
are historical identities, not the1.1 execution source.

| Frozen item | SHA256 |
|---|---|
| Candidate patch | `b80bdd16def4e0c0683945a6407ea6aeb0e6012652a753629d5037a3ac37ebba` |
| Executor | `69e3fac7b7ab9d43e3eca5886c7c3738fc7aaef64b80fbd2ae03b3c175c22d30` |
| Test binary | `e2253e69cbe0c031bf6846537ff8db88c2a8fe55dcfdb2a1d9eb57d795133bd6` |
| Independent A receipt | `bdab0859fe2a2568f240c11d6cc5f7317e167e5d5c68771243f8d37773129035` |
| Independent B receipt | `df20736d138c4ceb85371097fdbdab6b19cdb0ea0bd2f94a23e6fd83ff9ea99c` |

The policy is
`e0a8d176266d932a3a20a86e5c6d4b66ed03434abc776a5cd5b8652ed7ec0e7f`.
A new, read-only Rust identity check verified81 distinct named files, including
A's16 and B's22 evidence references, both binaries, original and derived inputs,
parent and four saved F/I natives, and26 generation raw files covering3,456
rows. It checked source/runtime/policy agreement, segment chaining, closed
common64 and native model/Adam/cursor identities. It did not construct or call
a model. This was a reference-scoped check, not a global artifact inventory.

## A acceptance, reused actual executions

The independent preparation reader reconstructs requests from original entity,
context and the unique current record, without calling the candidate
transformation. Its unchanged source, executable, inputs and receipt were
verified. All22,272 derived episodes were previously checked, including3,840
word transformations: each mode's train1536/dev192/renamed192.

| Contract check | Verified result and evidence class |
|---|---|
| FULL length | REUSED_EXECUTED_EVIDENCE: prompt218 + gold15 + EOS1 =234, all1,920 rows |
| ID_ONLY length | REUSED_EXECUTED_EVIDENCE: prompt233 + gold8 + EOS1 =242, all1,920 rows |
| Limit, target and evidence | 256 unchanged; violations0; provided2/excluded0; original tokenizer, FULL and review bytes preserved |
| Split and registered tape |192 train semantic groups /48 disjoint dev groups; renamed shares those48; original tape512..640 and F/I slots/exposure equal |
| Request/clock and strict scorer→gate | Matching executed tests1/1 each; wrong IDs, format, missing EOS, invalid UTF-8, raw/text mismatch and255/256/257 bounds covered |
| Actual Metal TINY native caller | Direct8 + independent8 optimizer/backward updates; each arm continuous2 equals1+fresh-process1 in weights, all Adam m/v, cursor and counters |
| Inherited optimizer | First new clock513; synthetic nonzero inherited moments retained; moved native accepted, wrong arm/runtime rejected |
| Actual teacher, both modes | Four TINY teacher calls total; independent forward/device/dtype/shift/roles readback; ID_ONLY has ID/EOS only |
| New dispatcher finalization | Actual fresh-process fixture with complete RETURNED, exhausted active budget and new calls0; native/raw preserved |

These are existing actual executions bound to the exact candidate. They were
not run again or counted as fresh GPU tests here. Child-process test output is
part of its parent regression, not additional distinct acceptance tests. No
zero-test invocation is counted. Length PASS alone was not used as A admission.
Generation receives the request through the existing native caller; the
request-only resolver and gold labels remain in preparation/scoring code.

## B and quality disposition

Both arms first saved model14849/Adam513/cursor1, then continued in a separate
process. Final F/I are each model14912/Adam576/cursor64, optimizer origin14336
and study start14848. Terminal success=true/resume=false; no +65 update exists.
I's S1Q1 has FULL57/64 and EOS60/64. Its four completed length-limit returns
trigger the registered error/non-EOS guard, even though FULL exceeds parent54.
F finished at the same64 endpoint. This is a normal quality stop.

This review ran the existing independent Rust endpoint and teacher readers
without model calls. Their new typed results are byte-identical to the
previous accepted results:

| New pure readback | SHA256 |
|---|---|
| Endpoint/raw/trace recount | `1ffe302f2a4b94aec7e0c4d7aa84093d2faae3470bdb3702ba8e8a08b35471e2` |
| Main256 + independent32 teacher recount | `b4c56306392ae8a6fb08e542cdfa1a013f9ab2a3ee49307cc9f33142de71bf90` |

The identity, endpoint and teacher commands all exited0. B's completed
normal32 + additional failure/mate32 per arm, generation128 total, and
teacher8/mode/arm32 were reused after identity verification. Original tokens,
text, EOS and errors matched, including wrong/length-ended responses. Teacher
gold/argmax/roles matched with maximum NLL delta0, against the1e-5 bound.
No further generation or teacher is necessary or counted this review.

Parent/F/I all have FULL and ID_ONLY exact0/192 on word and renamed; joint
QUERY_BOTH/ALL4 are0. F/I train exact0/128 and OLD_FULL exact0/64 remain failures.
ID_ONLY heldout outputs are all malformed; outside0 is not support success.
Its value-swap measure is selected-ID invariance, not generation of the swapped
word. Teacher gold-prefix scores do not increase free-generation accuracy.
The registered next-signal criterion fails; no architecture-wide impossibility
or future-learning success is inferred.

## Usage, preservation and publication

Existing total SMALL usage is optimizer/backward128, generation3456 and
teacher288. This includes B generation128 and teacher32. TINY is separately
optimizer/backward16, teacher4, generation0. This review adds **optimizer0,
backward0, generation0, teacher0 and model forward0**. It compiles one small
Rust file checker against existing cached dependencies and executes three pure
checks; it does not rebuild or retest the product, repeat SMALL learning, or
consume remaining generation budget. No dependencies or network models are used.

Actual per-arm64-update costs are F input104832/target7488/padding14464 and
I input106880/target5696/padding16512; exposure is review256+word256 each.
Equal updates/exposure are not equal token cost or FLOPs. The existing active
charge656.538534833seconds, generated tokens46948, teacher targets3600 and
scoped548945643-byte artifact snapshot are reused measurements, not new timing
or inventory claims. New files are limited to scratch verification and this
report. No new model/data artifacts or shared Cargo build outputs are created.
The new scratch alone contains13 files /2,880,677 logical bytes at closure;
this excludes the Markdown report and is not an allocated-size or peak claim.

Scratch: `artifacts/selected-event-id-v11-20260925-recheck/`.
`reuse_identity.rs` SHA256
`f129d21c0576f3e1a23d4732008965c622503265b2f2165c09278e87d04b7bfd`;
its binary SHA256
`cc0542881b45ee8615f67ade6d4d4d513a77b617c56c78bbd0e41546fb0d4b13`.
Commands are the checker with a new manifest output, existing `recount-b` with
a new endpoint readback path, and existing `teacher_reader` with
`final --with-independent` and a new teacher readback path. Logs are
`identity.log`, `endpoint-readback.log`, `teacher-readback.log`; compiler
diagnostics are in `build-identity.log`. Exact commands and observed exit0
statuses are in `commands.txt`. The81-file `preserved.sha256` and11-file
`source-history.sha256` both passed final verification. Earlier reports,1.0 failure,
original corpus/tokenizer/Adam and raw remain unchanged.

No further tests or nominal patches are requested for this completed scope.
S4/S5/S6, confirmation and Goal1 are not promoted. Only this report is published;
its containing commit is the report SHA, separate from candidate2e98f6a.
Actual remote SHA equality is checked after publication and stated in delivery.
