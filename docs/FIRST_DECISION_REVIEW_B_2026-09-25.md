# First-decision independent B — preliminary read-only check

The original v7 study remains failed and closed. This is a preliminary B check of saved evidence, not a final B receipt or model-quality decision. No optimizer, backward, generation, or teacher calls were made here.

The saved segment chain has C/W locals 1, 128, 256. Both final natives are model 18237/Adam 3901, with physical SHA256 C `1f5656d5c033a5cba3d271c3939aed66aaa14f4a00b11cc30f8081af5db28a90` and W `c7d9e767646173815b8c8afd45299067723512ada87b3a61beea1ee25830c3bd`. Segment 002 records `success=false`, `resume=false`, `fit=false`, `INTEGRITY_FAIL`, and `first-decision teacher two-ID group`; it records 768 generation and zero teacher calls. Its failed terminal remains unchanged. Source-only sampler correction `src/muon.rs` SHA256 `2490741b402c4e72f83b2db3d23e27492deb3cdf608decf9ec090ae397935f25` passed the actual-metadata zero-call selector test, but the frozen release executable is still SHA256 `e0ef2170a3604c408f41929ce4f727ac89c4b469a526f42c3ac6d2bb7a08e7c0`.

Direct read of each arm's three update streams found 1+127+128 committed rows, first source tape index 573 and last 828. Each arm's rows sum to input 411360, target 29952, padding 65824. Weighted denominator totals are C 29952 and W 30976, exactly four additional denominator units per W update. No discarded records were found. The study controls record baseline elapsed 8.137834209s, segments 16.152325459s, 330.16532725s, and 345.967694958s. Combined with A's charged 181.56s, observed and reserved active time is 881.983181876s of 7200s; untimed work and peak memory remain unknown.

Resolved generation records: C 1088 (128 panel 320, final panel 768), W 320 (128 panel only), total 1408 of 2560. Resolved teacher calls are zero of 208. The remaining registered final W panels require 768 generations; B permits up to 64 more generations and 16 teacher replays after 192 main teacher calls. No final W quality comparison is available yet. The parent64 baseline reused prior raw with zero new calls. The older posthoc parent B receipt SHA256 `0c4f06fb30d8224b9f3ad13019cd7a65b172c1e8446e5d1057423be68dbda27f` binds parent 17981, but its fixed normal32/failure-mate32 requests have not yet been established as the current fixed parent16.

One B replay selection defect is reproduced on actual C final raw. `first_review` in `src/muon.rs` iterates every failed FULL-word row and appends that row plus its next view without tracking already selected rows or pairs. Its first 16 planned failure/mate entries are indices `0,1,1,2,2,3,3,0,5,6,6,7,8,9,9,10`: only 10 distinct rows, all from FULL-word. It would waste six capped generation calls and provide duplicate replay evidence. Select distinct four-view groups or deduplicate complete failure/mate pairs, then bind the selected requests before new calls. This is an execution-review issue, not an observed candidate quality score.

CODE: sampler source correction checked; B replay selector needs repair. EXECUTION: original segment 002 FAIL; new evaluation NOT_RUN. RAW_INTEGRITY: preliminary trace/cost check passed, full panel recount pending. COST: current call/time totals within cap; storage/build growth/peak final determination pending. QUALITY/FOLLOWUP_SIGNAL: not assessed. GOAL1: not accepted.
